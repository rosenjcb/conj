import React, { useState } from 'react'
import { Root, FieldName } from './styles';

export const SubmitBox = (props) => {

  return(
    <Root>
      <FieldName>Hello World</FieldName>
    </Root>
  )
}